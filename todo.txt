1. На страничке с добавлением нового админа (её можно сделать один в один с логином в админа,
 просто немножко другой текст написать, типо "имя нового админа") кнопка "Добавить" будет
 fetch('api/v1/own/kolybPanelka/registerNewSubmit'), и обрабатывать два ответа, ResponseEntity.ok
 и .ResponseEntity.badRequest().body("fail"), я хуй знает что это за статус ответа, глянь этот момент короче;
При .ok - редиректить на админку('/api/v1/own/kolybPanelka'), при хуевом ответе, а он может быть только если ввели уже
существующий логин, просто выводи типо "Такой логин уже существует"

2.Редирект после успешного логина из /api/v1/own/kolybPanelka/loginPanel на админку /api/v1/own/kolybPanelka
Если ошибка - выводи её в качестве такой же штучки как ты сделал при ненажатой кнопки согласия в форме.

Главное чтобы все поля для доната, просьбе о помощи и прочего были заполнены полностью